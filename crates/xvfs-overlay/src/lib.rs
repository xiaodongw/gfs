//! Crash-safe copy-on-write overlay state machine.
//!
//! **Declared placeholder, owned by M3.** PLAN.md M1.1 lists this crate as part
//! of the workspace deliverable, so it exists to fix the workspace shape and give
//! M3 a home rather than to hold M1 code. It is empty on purpose; an empty crate
//! that says which milestone fills it is easier to review than a crate that
//! appears mid-milestone with no recorded place in the plan.
//!
//! Scope when M3 starts: Path state machine for base, copied-up, created, deleted, renamed, and type-changed entries, plus the journal transaction ordering M3.1 specifies.
