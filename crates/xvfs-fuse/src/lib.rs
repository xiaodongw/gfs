//! FUSE filesystem adapter.
//!
//! **Declared placeholder, owned by M2.** PLAN.md M1.1 lists this crate as part
//! of the workspace deliverable, so it exists to fix the workspace shape and give
//! M2 a home rather than to hold M1 code. It is empty on purpose; an empty crate
//! that says which milestone fills it is easier to review than a crate that
//! appears mid-milestone with no recorded place in the plan.
//!
//! Scope when M2 starts: The fuser::Filesystem implementation, inode model, blob cache, and the synthesized read-only .git surface ADR 0005 selected.
